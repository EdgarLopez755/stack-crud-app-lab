<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= food.name %></title>
</head>
<body>
    <a href="/">Homepage</a>
    <a href="/foods/new">Add New Food</a>
    <a href="/foods/">Back to Foods</a>
    <h1><%= food.name %></h1>
    <% if(food.isReadyToEat) { %> 
        <p> This Food is ready to EAT! </p>
    <% } else { %>
        <p> This food is NOT ready to eat!</p>
    <% } %>
    <form action="/foods/<%food.id%>?_method=DELETE" method="POST">
        <button type="submit">Delete <%= food.name %></button>
    </form>
    <a href="/foods/<%= food.id %>/edit">Edit <%= food.name %></a>
    <a href="/foods">Back to foods</a>
    
</body>
</html>